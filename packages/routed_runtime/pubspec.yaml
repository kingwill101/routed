name: routed_runtime
description: Runtime environment, shutdown, and warning helpers for the Routed ecosystem.
version: 0.1.0
repository: https://github.com/kingwill101/routed/tree/master/packages/routed_runtime
tags:
  - routed
  - runtime
  - env

funding:
  - https://www.buymeacoffee.com/kingwill101

resolution: workspace

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  dotenv: ^4.2.0
  json_schema_builder: ^0.1.3
  meta: ^1.18.0
  routed_core: ^0.1.0
  server_contracts: ^0.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.29.0
