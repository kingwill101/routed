diff --git a/packages/relic_io/lib/src/adapter/bind_http_server.dart b/packages/relic_io/lib/src/adapter/bind_http_server.dart
index e93b47e..04a50e2 100644
--- a/packages/relic_io/lib/src/adapter/bind_http_server.dart
+++ b/packages/relic_io/lib/src/adapter/bind_http_server.dart
@@ -1,6 +1,12 @@
 import 'dart:async';
 import 'dart:io' as io;
 
+import 'package:server_native/server_native.dart';
+
+const _serverNativeCompatEnv = 'SERVER_NATIVE_COMPAT';
+bool get _useServerNative =>
+    io.Platform.environment[_serverNativeCompatEnv] == 'true';
+
 /// Binds an HTTP server to the given [address] and [port].
 ///
 /// If [context] is provided, a secure HTTPS server will be started using
@@ -28,13 +34,22 @@ Future<io.HttpServer> bindHttpServer(
   final bool shared = false,
 }) async {
   if (context == null) {
-    return await io.HttpServer.bind(
-      address,
-      port,
-      backlog: backlog,
-      v6Only: v6Only,
-      shared: shared,
-    );
+    return await (_useServerNative
+        ? NativeHttpServer.bind(
+            address,
+            port,
+            backlog: backlog,
+            v6Only: v6Only,
+            shared: shared,
+            http3: false,
+          )
+        : io.HttpServer.bind(
+            address,
+            port,
+            backlog: backlog,
+            v6Only: v6Only,
+            shared: shared,
+          ));
   }
   return await io.HttpServer.bindSecure(
     address,
diff --git a/packages/relic_io/pubspec.yaml b/packages/relic_io/pubspec.yaml
index 99f2f96..2c56397 100644
--- a/packages/relic_io/pubspec.yaml
+++ b/packages/relic_io/pubspec.yaml
@@ -19,6 +19,7 @@ dependencies:
   mime: ^2.0.0
   path: ^1.8.3
   relic_core: ^1.2.0
+  server_native: ^0.1.0
   stack_trace: ^1.10.0
   stream_channel: ^2.1.1
   web_socket: ^1.0.1
diff --git a/packages/relic_io/test/web_socket/web_socket_test.dart b/packages/relic_io/test/web_socket/web_socket_test.dart
index fcc63ce..f0ede48 100644
--- a/packages/relic_io/test/web_socket/web_socket_test.dart
+++ b/packages/relic_io/test/web_socket/web_socket_test.dart
@@ -12,6 +12,10 @@ import 'package:web_socket_channel/web_socket_channel.dart';
 
 import '../util/test_util.dart';
 
+const _serverNativeCompatEnv = 'SERVER_NATIVE_COMPAT';
+bool get _useServerNative =>
+    io.Platform.environment[_serverNativeCompatEnv] == 'true';
+
 RelicServer? _server;
 int get _serverPort => _server!.url.port;
 
@@ -353,11 +357,11 @@ void main() {
       );
       clientSocket.pingInterval = pingInterval;
 
-      final check = expectLater(
-        clientSocket.events,
-        emitsInOrder([
+        final check = expectLater(
+          clientSocket.events,
+          emitsInOrder([
           TextDataReceived('running'),
-          CloseReceived(1001),
+          CloseReceived(_useServerNative ? 1006 : 1001),
           emitsDone,
         ]),
       );
