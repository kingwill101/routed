/// Base file used by build_runner to generate GeckoDriver version constants.
///
/// This file itself contains no implementation but serves as an input for the
/// builder that generates `gecko_driver_version.g.dart`.
library;
