/// Base file used by build_runner to generate ChromeDriver version constants.
///
/// This file itself contains no implementation but serves as an input for the
/// builder that generates `chrome_driver_version.g.dart`.
library;
