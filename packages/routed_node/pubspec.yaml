name: routed_node
description: Node.js host transport for Routed — IncomingMessage/ServerResponse adapters on RequestAdapter/ResponseAdapter/ServerTransport.
version: 0.1.0
repository: https://github.com/kingwill101/routed/tree/master/packages/routed_node
tags:
  - routed
  - server
  - node
  - js_interop

funding:
  - https://www.buymeacoffee.com/kingwill101

resolution: workspace

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  crypto: ^3.0.0
  routed_core: ">=0.4.0 <1.0.0"
  web: ^1.1.1

dev_dependencies:
  lints: ^6.0.0
  routed_auth: any
  test: ^1.29.0
  routed_node_api_sample: any
  routed_sessions: any
