name: routed_auth
description: Routed-specific authentication integration package built on top of server_auth.
version: 0.2.0
repository: https://github.com/kingwill101/routed/tree/master/packages/routed_auth
tags:
  - auth
  - routed
  - server

funding:
  - https://www.buymeacoffee.com/kingwill101

resolution: workspace

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  http: ^1.6.0
  routed_core: ">=0.4.0 <1.0.0"
  server_auth: ">=0.2.0 <1.0.0"
  routed_sessions: ">=0.2.0 <1.0.0"
  server_sessions: ">=0.1.0 <1.0.0"
  routed_views: ">=0.2.0 <1.0.0"
  pointycastle: ^4.0.0
  routed_http: ">=0.1.0 <1.0.0"

  crypto: ^3.0.0
dev_dependencies:
  jose: ^0.3.5
  lints: ^6.0.0
  property_testing: ^0.3.2
  routed_testing: ">=0.4.0 <1.0.0"
  server_testing: ">=0.4.0 <1.0.0"
  test: ^1.29.0
  # removed: server_data: any (split into server_sessions/cache etc.)
  routed: any
  file: any
