name: routed_auth
description: Routed-specific authentication integration package built on top of server_auth.
version: 0.1.0
repository: https://github.com/kingwill101/routed/tree/master/packages/routed_auth
tags:
  - auth
  - routed
  - server

funding:
  - https://www.buymeacoffee.com/kingwill101

resolution: workspace

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  http: ^1.6.0
  json_schema_builder: ^0.1.3
  routed: ^0.3.3
  server_auth: ^0.1.0
  routed_sessions: ^0.1.0
  server_sessions: ^0.1.0
  routed_view: ^0.1.0

dev_dependencies:
  jose: ^0.3.5
  lints: ^6.0.0
  # removed: property_testing: ^0.3.2 (workspace package no longer vendored)
  # removed: routed_testing: ^0.3.2 (workspace package no longer vendored)
  # removed: server_testing: ^0.3.2 (workspace package no longer vendored)
  test: ^1.29.0
  # removed: server_data: any (split into server_sessions/cache etc.)
