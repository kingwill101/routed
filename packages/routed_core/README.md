# routed_core

Slim HTTP engine for the Routed ecosystem (`Engine`, `EngineContext`, `Router`).

Applications should depend on [`package:routed`](../routed). Adapter packages
depend on `routed_core` directly.
