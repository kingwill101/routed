# jaspr_routed Example

Simple app that mounts jaspr components inside a Routed engine using the
`jaspr_routed` adapter. Run it while developing jaspr views to verify hydration
and routing hooks.

```bash
dart pub get
dart run bin/server.dart
```

Not published to pub.dev—it's a local playground.
