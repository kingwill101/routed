name: jaspr_routed
description: Jaspr integration for the Routed backend.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.9.0 <4.0.0"

resolution: workspace

dependencies:
  jaspr: ^0.21.0
  routed:
    path: ../routed

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.7
