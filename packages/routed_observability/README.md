# routed_observability

Observability providers/middleware for Routed (tracing, metrics, health).

Extracted from `routed/src/observability/*` (`errors.dart`, `health.dart`, `metrics.dart`, `tracing.dart`).

## Install
```yaml
dependencies:
  routed: ^0.3.3
  routed_observability: ^0.1.0
```
