name: routed_io
description: dart:io HTTP(S) host transport for Routed — kept separate from routed_core for Cloudflare Workers and other non-dart:io hosts.
version: 0.1.0
repository: https://github.com/kingwill101/routed/tree/master/packages/routed_io
tags:
  - routed
  - server
  - io

funding:
  - https://www.buymeacoffee.com/kingwill101

resolution: workspace

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  routed_core: ^0.3.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.29.0
