include: package:very_good_analysis/analysis_options.yaml

linter:
  rules:
    # Validation messages and regex literals are often clearer when kept on
    # one line; the formatter still governs the surrounding Dart code.
    lines_longer_than_80_chars: false
