name: routed_security
description: Security providers for Routed (CORS, IP filtering, trusted proxies, headers, and request size).
version: 0.1.1
repository: https://github.com/kingwill101/routed
funding:
  - https://www.buymeacoffee.com/kingwill101
environment:
  sdk: ">=3.12.0 <4.0.0"
resolution: workspace
dependencies:
  routed_core: ">=0.4.0 <1.0.0"
dev_dependencies:
  test: ^1.29.0
  very_good_analysis: ^10.0.0
