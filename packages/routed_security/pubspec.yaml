name: routed_security
description: Security bundle for Routed (IP filter, trusted proxies, headers, CSRF, request size).
version: 0.1.0
repository: https://github.com/kingwill101/routed
environment:
  sdk: ">=3.9.0 <4.0.0"
resolution: workspace
dependencies:
  routed: ^0.3.3
dev_dependencies:
  lints: ^6.0.0
  test: ^1.29.0
