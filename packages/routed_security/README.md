# routed_security

Security bundle for Routed (IP filter, trusted proxies, headers, CSRF, request size).

Extracted from `routed/src/security/*` (`ip_filter.dart`, `network.dart`, `trusted_proxy_resolver.dart`).

## Install
```yaml
dependencies:
  routed: ^0.3.3
  routed_security: ^0.1.0
```
