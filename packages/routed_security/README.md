# routed_security

Security primitives for Routed, including:

- CIDR/IP matcher utilities
- IP allow/deny filter model
- Trusted proxy client-IP resolver

This package is intended for modular Routed installs and for umbrella usage via
`package:routed`.
