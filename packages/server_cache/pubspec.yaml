name: server_cache
description: Framework-agnostic cache runtime for server ecosystems (portable, from server_data/cache).
version: 0.2.1
repository: https://github.com/kingwill101/routed/tree/master/packages/server_cache
tags:
  - cache
  - server

funding:
  - https://www.buymeacoffee.com/kingwill101

resolution: workspace

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  crypto: ^3.0.0
  file: ^7.0.1
  redis: ^4.0.0
  server_contracts: ">=0.1.0 <1.0.0"
  storage_fs: ^0.1.0

dev_dependencies:
  coverage: ^1.15.1
  test: ^1.29.0
  very_good_analysis: ^10.0.0
