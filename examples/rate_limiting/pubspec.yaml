name: rate_limiting_example
description: Demonstrates rate limiting with token bucket, sliding window, and quota strategies.
publish_to: none
funding:
  - https://www.buymeacoffee.com/kingwill101

environment:
  sdk: ">=3.12.0 <4.0.0"

resolution: workspace

dependencies:
  ormed: ^0.4.0
  ormed_sqlite: ^0.4.1
  routed:
  http: ^1.5.0

tags:
  - example
  - routed
