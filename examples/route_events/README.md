# Route Events Example

Streams lifecycle events from Routed's `SignalHub` so you can see route start,
finish, and error hooks in action.

```bash
dart pub get
dart run bin/server.dart
```

Use it to explore observability patterns without touching production code.
