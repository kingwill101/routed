# HTTP/2 Example

Demonstrates how to boot a Routed engine with HTTP/2 enabled including TLS
setup and request handlers that stream responses.

```bash
dart pub get
dart run bin/server.dart --config config/http2.yaml
```

Use it as a local playground; it's not distributed on pub.dev.
