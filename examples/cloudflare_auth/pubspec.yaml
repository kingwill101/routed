name: routed_cloudflare_auth_example
description: A runnable D1-backed auth Worker built with Routed.
publish_to: none
version: 0.1.0

resolution: workspace

environment:
  sdk: '>=3.9.0 <4.0.0'

dependencies:
  liquify: ^1.6.1
  routed: '>=0.5.0 <1.0.0'
  routed_auth: '>=0.2.0 <1.0.0'
  routed_auth_cloudflare: '>=0.1.0 <1.0.0'
  routed_core: '>=0.4.0 <1.0.0'
  routed_node: '>=0.2.1 <1.0.0'
  routed_sessions: '>=0.2.0 <1.0.0'
  routed_views: '>=0.2.0 <1.0.0'
  server_auth: '>=0.2.0 <1.0.0'

dev_dependencies:
  lints: ^6.0.0
  routed_cli: '>=0.3.0 <1.0.0'
  routed_auth_sqlite: '>=0.1.0 <1.0.0'
  routed_testing: '>=0.4.0 <1.0.0'
  server_testing: '>=0.4.0 <1.0.0'
  test: ^1.29.0
