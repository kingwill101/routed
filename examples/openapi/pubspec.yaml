name: routed_app1
description: A new Routed application.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ">=3.9.2 <4.0.0"
resolution: workspace
dependencies:
  args: ^2.5.0
  routed:
    path: ../../packages/routed

dev_dependencies:
  lints: ^6.0.0
  test: ^1.29.0
  hotreloader: ^4.3.0
