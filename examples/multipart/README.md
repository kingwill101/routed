# Multipart Example

Demonstrates multipart uploads, binding helpers, and validation utilities in
Routed.

```bash
dart pub get
dart run bin/server.dart
```

Comes bundled as a learning aid—fork it to test your own upload flows.
