name: kitchen_sink_example
publish_to: none
environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  routed:
  http: ^1.3.0
  uuid: ^4.5.1
  file: ^7.0.1
  jinja: ^0.6.0
  collection:
dev_dependencies:
  routed_testing:
  server_testing:
