# Liquid Template Example

Shows how to render Liquid templates via Routed's view helpers and form
handling pipeline.

```bash
dart pub get
dart run bin/server.dart
```

Use this as a reference when wiring template engines into your apps.
