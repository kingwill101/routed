# Fallback Example

Shows how to build fallback routes and grouped error handlers with Routed. Use
it as a reference while experimenting with routing edge cases.

```bash
dart pub get
dart run bin/server.dart
```

Not intended for publishing—it's an educational sample bundled with the repo.
