name: routed_cloudflare_jobs_example
description: A live Cloudflare Worker Queue and Cron example built with Routed.
publish_to: none
version: 0.1.0

resolution: workspace

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  routed: '>=0.5.0 <1.0.0'
  routed_jobs: '>=0.1.0 <1.0.0'
  routed_node: '>=0.2.1 <1.0.0'

dev_dependencies:
  lints: ^6.0.0
  test: ^1.29.0
