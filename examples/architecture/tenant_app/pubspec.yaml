name: routed_architecture_tenant_app
description: Routed tenant, principal, and gate architecture example.
publish_to: none
version: 0.1.0

resolution: workspace

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  ormed: ^0.4.0
  ormed_sqlite: ^0.4.1
  routed:
    path: ../../../packages/routed
  routed_auth:
    path: ../../../packages/routed_auth
  routed_database:
    path: ../../../packages/routed_database
  routed_sessions:
    path: ../../../packages/routed_sessions
  routed_io:
    path: ../../../packages/routed_io

dev_dependencies:
  lints: ^6.0.0
  routed_testing:
    path: ../../../packages/server_testing/routed_testing
  server_testing: '>=0.4.0 <1.0.0'
  test: ^1.29.0
