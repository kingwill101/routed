name: routed_architecture_cloudflare_d1
description: Routed Cloudflare D1 database architecture example.
publish_to: none
version: 0.1.0

resolution: workspace

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  ormed: ^0.4.0
  routed:
    path: ../../../packages/routed
  routed_database:
    path: ../../../packages/routed_database
  routed_node:
    path: ../../../packages/routed_node

dev_dependencies:
  lints: ^6.0.0
