name: error_responses_example
description: Demonstrates content-negotiated error responses (JSON, HTML, plain text).
publish_to: none
funding:
  - https://www.buymeacoffee.com/kingwill101

environment:
  sdk: ">=3.9.0 <4.0.0"

resolution: workspace

dependencies:
  routed:
  http: ^1.5.0

tags:
  - example
  - routed
