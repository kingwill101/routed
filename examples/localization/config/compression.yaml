# Compression configuration quick reference:
# algorithms – Preferred compression algorithms (gzip, br). Default: ["br","gzip"]. Type: list<string>
# enabled – Enable automatic response compression. Default: true. Type: bool
# mime_allow – MIME prefixes eligible for compression. Default: ["text/*","application/json","application/javascript"]. Type: list<string>
# mime_deny – MIME prefixes excluded from compression. Default: ["image/*","audio/*","video/*"]. Type: list<string>
# min_length – Minimum body size (bytes) before compression applies. Default: 1024. Type: int

algorithms:
  - br
  - gzip
enabled: true
mime_allow:
  - "text/*"
  - application/json
  - application/javascript
mime_deny:
  - "image/*"
  - "audio/*"
  - "video/*"
min_length: 1024

