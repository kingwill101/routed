# View configuration quick reference:
# cache – Enable template caching in production environments. Default: true. Type: bool
# directory – Path to templates relative to app root or disk. Default: views. Type: string
# disk – Optional storage disk to source templates from. Type: string|null
# engine – View engine identifier (e.g. liquid). Default: liquid. Type: string

cache: true
directory: views
engine: liquid

