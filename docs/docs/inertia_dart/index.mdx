---
title: Inertia Dart
description: Server-side Inertia.js protocol for Dart with setup guides and reference docs
sidebar_position: 0
---

# Inertia Dart

Build single-page apps without building an API. `inertia_dart` implements the
server-side Inertia protocol for Dart and provides property helpers, SSR
support, asset manifest reading, and testing utilities. Pair it with a client
adapter like `@inertiajs/react`, `@inertiajs/vue3`, or `@inertiajs/svelte`.

> **Want a hands-on walkthrough?** Follow the [Build a Contacts App](/docs/tutorial) tutorial using just `dart:io`.

## Start here

- [Getting Started](/docs/getting-started) for installation, the CLI, and setup choices
- [HttpServer](/docs/httpserver) for the plain `dart:io` path
- [Serinus](/docs/serinus) for the `serinus_inertia` adapter
- [Routed](/docs/routed/) for the `routed_inertia` adapter

## Core topics

- [Props](/docs/props) for property helpers and merge behavior
- [Partial Reloads](/docs/partial-reloads) for request headers and `PropertyContext`
- [Assets](/docs/assets) for Vite manifests, hot files, and HTML rendering
- [SSR](/docs/ssr) for gateways, renderer processes, and CLI helpers
- [Testing](/docs/testing) for response assertions and protocol verification

## What the package includes

- Full Inertia protocol handling for HTML and JSON responses
- Property helpers like `LazyProp`, `DeferredProp`, `MergeProp`, and `ScrollProp`
- Request helpers for partial reloads, deferred groups, and merge resets
- Vite asset helpers for development and production
- SSR helpers for HTTP gateways and renderer lifecycle management
- Testing utilities with `AssertableInertia`

## Reference apps

- [`packages/inertia/example/inertia_client_app`](https://github.com/kingwill101/routed/tree/main/packages/inertia/example/inertia_client_app)
- [`packages/inertia/example/inertia_ssr_app`](https://github.com/kingwill101/routed/tree/main/packages/inertia/example/inertia_ssr_app)

## Vite hot file plugin

The package ships a Vite plugin that writes a `public/hot` file on dev server
start (containing the dev server URL) and deletes it on shutdown. Copy
[`assets/vite/inertia_hot_file.js`](https://github.com/kingwill101/routed/tree/main/packages/inertia/assets/vite/inertia_hot_file.js)
into your client project:

```js
import { defineConfig } from 'vite'
import { inertiaHotFile } from './inertia_hot_file.js'

export default defineConfig({
  plugins: [inertiaHotFile()],
})
```

## Example apps

- [`packages/inertia/example/inertia_client_app`](https://github.com/kingwill101/routed/tree/main/packages/inertia/example/inertia_client_app) -- standalone `dart:io` server with client-side Inertia
- [`packages/inertia/example/inertia_ssr_app`](https://github.com/kingwill101/routed/tree/main/packages/inertia/example/inertia_ssr_app) -- standalone `dart:io` server with SSR

## Learn more

- [Inertia.js docs](https://inertiajs.com) -- protocol specification and client adapters
- [HttpServer guide](/docs/httpserver) -- plain `dart:io` setup from scratch
- [Serinus guide](/docs/serinus) -- adapter-based setup with `serinus_inertia`
- [Routed Inertia](/docs/routed/) -- framework integration for Routed apps
