name: bench_routed
version: 0.1.0
description: Baseline routed server for benchmarks.
publish_to: none
environment:
  sdk: ">=3.12.0 <4.0.0"
dependencies:
  routed_core:
    path: ../../../packages/routed_core
dependency_overrides:
  server_contracts:
    path: ../../../packages/server_contracts
